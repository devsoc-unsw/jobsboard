1) gyp ERR! find Python 
solution:
    - npm install -g node-gyp 
    - docker compose build

2) failed to solve: rpc error: code = Unknown desc = executor failed running [/bin/sh -c yarn install --frozen-lockfile]: exit code: 1
solution:

3) jobs-board-frontend@0.1.0: The engine "node" is incompatible with this module. Expected version "16.14.0". Got "17.8.0"
solution:
    - nvm use --delete-prefix v16.14.0
    - most likely not needed but, nvm use 16.14.0
